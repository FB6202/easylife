@org.springframework.modulith.ApplicationModule(
        displayName = "Todos",
        allowedDependencies = {
                "users",
                "categories",
                "shared"
        })

package com.easylife.app.todos;