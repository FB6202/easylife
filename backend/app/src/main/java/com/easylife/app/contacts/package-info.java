@org.springframework.modulith.ApplicationModule(
        displayName = "Contacts",
        allowedDependencies = {
                "users",
                "categories",
                "shared"
        })

package com.easylife.app.contacts;